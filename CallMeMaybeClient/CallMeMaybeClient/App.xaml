<Application x:Class="CallMeMaybeClient.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="Views/SalariePage.xaml">
    <Application.Resources>
        <!-- Définition des couleurs -->
        <Color x:Key="PrimaryYellowColor">#FFC107</Color>
        <Color x:Key="AccentWhiteColor">#FFFFFF</Color>

        <!-- Ressources de brosses -->
        <SolidColorBrush x:Key="PrimaryYellow" Color="{StaticResource PrimaryYellowColor}" />
        <SolidColorBrush x:Key="AccentWhite" Color="{StaticResource AccentWhiteColor}" />

        <!-- Style pour le bouton avec Coin Arrondi -->
        <Style TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryYellow}" />
            <Setter Property="Foreground" Value="{StaticResource AccentWhite}" />
            <Setter Property="BorderBrush" Value="{StaticResource PrimaryYellow}" />
            <Setter Property="BorderThickness" Value="2" />
            <Setter Property="Padding" Value="10,5" />
            <Setter Property="FontSize" Value="16" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border CornerRadius="8"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Application.Resources>
</Application>

